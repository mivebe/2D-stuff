# WebGL2 Shader Gallery

A collection of GLSL fragment shaders rendered on a fullscreen quad with raw WebGL2.
Pick a shader from the dropdown; each one reads some subset of the `u_resolution`,
`u_time`, and `u_mouse` uniforms.

### Install
```
npm install
```

### Start
```
npm run start
```

### Build
```
npm run build
```
